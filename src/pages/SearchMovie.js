import React from 'react';

//template
import SearchMovieTemplate from '../templates/SearchMovieTemplate';

const SearchMovie = ({navigation}) => {
  return <SearchMovieTemplate navigation={navigation} />;
};

export default SearchMovie;
